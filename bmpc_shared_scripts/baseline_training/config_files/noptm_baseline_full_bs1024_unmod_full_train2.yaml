dataset:
  name: noptm_baseline_full_bs1024_unmod
  hf_home: /cmnfs/proj/bmpc_dlomix/datasets
  hf_cache: /cmnfs/proj/bmpc_dlomix/datasets/hf_cache
  parquet_path: /cmnfs/data/proteomics/Prosit_unmod/intensity/no_aug
  processed_path: /cmnfs/proj/bmpc_dlomix/datasets/processed/noptm_baseline_full_bs1024_unmod
  seq_length: 30
  batch_size: 1024
  alphabet: ALPHABET_UNMOD
training:
  learning_rate: 0.001
  num_epochs: 60
  early_stopping:
    min_delta: 0.00001
    patience: 8
  lr_warmup_linear:
    num_epochs: 6
    start_lr: 0.00001
    end_lr: 0.001
model:
  save_dir: /cmnfs/proj/bmpc_dlomix/models/baseline_models